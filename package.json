{
  "name": "ceylon-gold-export-mern",
  "version": "1.0.0",
  "private": true,
  "description": "Full-Stack MERN Application for Sri Lankan Agricultural Produce & King Coconut Exports",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd backend && npm install && cd ../frontend && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.1",
    "nodemailer": "^6.9.13"
  }
}
